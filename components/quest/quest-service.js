const questModel = require('./quest-model');
const ApiError = require('../../exceptions/api-error');
const userModel = require('../user/user-model');

class questService {

    async activate(user, name) {

        return quests;
    }
}

module.exports = new questService();
