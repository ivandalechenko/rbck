const Router = require('express').Router;
const tapController = require('./tap-controller');
const router = new Router();


// router.get('/taps', tapController.getAll);
// router.post('/taps', tapController.add);
// router.delete('/taps', tapController.delete);
// router.put('/taps', tapController.update);


module.exports = router
